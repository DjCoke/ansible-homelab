# Host specific
# These settings are unique to this particular host.

# Server settings for this host
docker_server: true
# Enables the Docker server role on this host, indicating that Docker should be installed and managed.

portainer_server: true
# Enables the Portainer server role on this host, indicating that Portainer, a Docker management tool, should be installed and managed.

code_server: true
# Enables the Code-Server role on this host, indicating that Code-Server, a web-based IDE, should be installed and managed.

hostname: docker.local.glind.io